import React from "react";
import dialogs from "./Dialogs.module.css";

const Dialogs = (props) =>  {
    return (
        <div className={dialogs.about}>
            Dialogs
        </div>
    );
};

export default Dialogs;